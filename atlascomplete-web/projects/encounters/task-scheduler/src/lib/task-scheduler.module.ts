import { NgModule } from '@angular/core';
import { TaskSchedulerComponent } from './task-scheduler.component';

@NgModule({
  declarations: [TaskSchedulerComponent],
  imports: [
  ],
  exports: [TaskSchedulerComponent]
})
export class TaskSchedulerModule { }
